# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema: {}               # no configuration options
  commandFunction: |-
    (config) => ({
      command: "node",
      args: ["index.js"],
      env: {}
    })
